#include <iostream>

using std::string;

string getOS();
string dbRead();
string promptString(string prompt);

void dbWrite();
void clearTerm();
void notify();
int id_Randomizer();
int promptInt(string prompt);

bool optionValid(int option, int max);
